# AGENTS.md - Workspace Agent Rules

## Project Scope
- Repository: `overtime-js`
- Primary Branch: `miiovt-main`

## Rules & Standards
- Keep calculations in sync with local labor and tax regulations (TER rules in `src/terData.js`).
- Test changes using `npm test` or `npm run build` before committing.
- Maintain modern Tailwind styling for all component UI elements.
